# Mirror of [project.dependencies] in pyproject.toml. Use this when running
# without uv: `pip install -r requirements.txt` then `python -m <service>`
# from the repo root (no `pip install -e .` needed — flat-layout monorepo).

psycopg[binary,pool]>=3.2
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
tenacity>=9.0
pydantic>=2.9
pydantic-settings>=2.5
apscheduler>=3.10
structlog>=24.4
mcp>=1.0
scipy>=1.14
numpy>=2.1
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth>=2.34
sentry-sdk>=2.14
click>=8.1
