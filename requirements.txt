# Mirror of [project.dependencies] in pyproject.toml. Use this when running
# without uv: `pip install -r requirements.txt` then `python -m <service>`
# from the repo root (no `pip install -e .` needed — flat-layout monorepo).

psycopg[binary,pool]>=3.2
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
tenacity>=9.0
pydantic>=2.9
pydantic-settings>=2.5
apscheduler>=3.10
structlog>=24.4
mcp>=1.0
scipy>=1.14
numpy>=2.1
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth>=2.34
sentry-sdk>=2.14
click>=8.1
boto3>=1.35
# Optional OTLP tracing (Langfuse Cloud / Grafana Cloud free tiers). Imported
# lazily by mcp_server.telemetry; no-op if LIFEOS_OTLP_ENDPOINT is unset.
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
